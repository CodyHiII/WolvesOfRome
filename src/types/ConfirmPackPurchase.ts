export type ConfirmPackPurchaseType = {
  selectedPack: any;
  itemId: string;
  packTitle: string;
  packsAmount: string | number;
  packImage?: string;
  price?: string | number;
  currency?: string;
};
